// v4.0: broadcaster data now lives in /broadcast-data/*.json.
// Keep this file only as a compatibility shim for older imports.
export const BROADCASTING_VERSION = "4.0";
export const BROADCASTS = {};
export default BROADCASTS;
